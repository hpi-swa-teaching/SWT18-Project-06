setup
descriptionBoxContent

	^ '<b>Groundhog Day</b><br>
We now need to implement what our morph should do on every step. That can be done by overriding the <a href="code://ColorfulMorph >> #step">step</a> method.<br>
<br>
Make the color change on every <a href="code://ColorfulMorph >> #step">step</a> and see what happens.<br>
<br>
Make sure you implemented <a href="code://ColorfulMorph >> #switchColors">switchColors</a> and <a href="code://ColorfulMorph >> #stepTime">stepTime</a> correctly.' asTextFromHtml